# Tutorial Rules

この文書は、キューブTutorialの問題定義・記法・判定ルールを定める共通仕様です。

## Piece and Sticker Notation

- 3文字の名前はコーナーパーツを表す。例: `ULF corner`
- 2文字の名前はエッジパーツを表す。例: `UB edge`
- パーツを指定するときは、最初の1文字が追跡するステッカーを表す。残りの文字がパーツの位置を表す。
- そのため、コーナーパーツを指定する `ULF corner` では、最初の `U` はステッカー情報を兼ねている。`U sticker` と重ねて書かない。
- `UB` と `BU` は同じUBエッジ上の別のステッカーを表す。

### Position Problems

`ULF corner -> URB corner` は、ULFコーナーパーツをURBコーナー位置へ移す問題を表す。向きは問わない。

Position問題では、パーツ名に `corner` または `edge` を付ける。

### Sticker Problems

`ULF -> URB` は、ULFコーナーのUステッカーをURB位置のU面へ移す問題を表す。

`LUF -> RBU` は、LUFコーナーのLステッカーをRBU位置のR面へ移す問題を表す。

Sticker問題では、パーツ種別を付けず、始点と終点の文字をそれぞれステッカー情報として解釈する。

Position問題はSticker問題より低難度、Sticker問題は高難度とする。

## Condition Vocabulary

問題の条件は、次の英語表現で統一する。

- `Start`: 問題開始時の状態または開始対象
- `Goal`: 目的地、またはゴール時に満たす最終条件
- `Via`: ゴールまでに指定位置を一度以上通過する条件
- `Fix`: 試行中に指定パーツ・ステッカーを一度も動かさない条件
- `Restore`: 試行中に動いてもよいが、ゴール時に開始時の位置・向きへ戻っている条件

`Fix` は経路全体の不変条件であり、`Restore` はゴール時だけ確認する終端条件である。

### Viaの判定

Sticker問題の `Via ULF` は、追跡中のステッカーがULF位置のU面へ一度以上来る条件を表す。
追跡対象のコーナーパーツがULF位置へ入っただけでは成立せず、ステッカーの向きも一致する必要がある。

`Via` の成立履歴は、現在残っている手順を先頭から適用した各状態を使って判定する。
一度成立した `Via` はその後ステッカーが別の位置へ移っても維持されるが、`Previous` で通過手まで取り消した場合は未成立へ戻る。

### Fixの判定

`Fix` は、指定対象を動かす操作を禁止する条件である。指定対象を含む層を一度でも回すことはできず、後続手で元へ戻せるかどうかは考慮しない。

- Fix違反となる操作は盤面へ適用しない。
- 違反操作のアニメーションは再生せず、手順履歴と手数にも加えない。
- 違反操作では `Via` などの経路条件も更新しない。
- 2層回しやスライス回転も、指定対象を動かす場合はFix違反とする。
- 連続手順を扱う場合も基本手ごとに検査し、最初の違反手より後は適用しない。

Fix対象を全く動かさないため、対象をPositionとして指定してもStickerとして指定しても判定結果は同じになる。
実装では手の名前を列挙して禁止するのではなく、その手がFix対象の位置または向きを変えるかで判定する。

### Restoreの判定

`Restore UB` は、開始時にUB位置のU面にあるステッカーが、Goal成立時に再びUB位置のU面へ戻っている条件を表す。
これにより対象パーツの位置と向きの両方が開始状態と一致する。

Restoreは必ずSticker条件として表記し、`corner` または `edge` を付けない。例えば `Restore UB edge` ではなく `Restore UB` と書く。

### Previousの判定

`Previous` で取り消した手は、最初から行われなかったものとして扱う。盤面、手順履歴、手数、`Via` とその他の経路条件は、取り消し後に残った手順だけから決まる。

## Tutorial Flow

- 各問題は完成状態から独立して開始する。
- 問題は最初からすべて解放し、クリア済み問題にも再挑戦できる。
- 問題の順序は問題定義配列の順序で決まり、配列を並べ替えるだけで変更できるようにする。
- 操作に時間制限・手数制限は設けない。
- 手数をスコアとして表示する。
- Goalを満たした瞬間に成功とする。
- 成功後は短時間だけ操作を無効にし、その後は再び操作できるようにする。
- 成功記録は保持する。ただし成功後に盤面を崩した場合、現在のGoal状態表示は失敗へ戻す。
- `Reset` は盤面、手順履歴、`Via` の通過履歴を初期状態へ戻す。
- `Previous` は1手前へ戻し、残った手順だけを使って `Via` とその他の経路条件を再計算する。

## Visual Rules

- Position問題では、対象パーツだけを色付きで表示し、その他のステッカーは灰色にする。
- Sticker問題では、対象ステッカーだけを色付きで表示し、その他のステッカーは灰色にする。
- Goal対象とGoal位置のエフェクトは区別する。
- `Via` の通過位置、`Fix` の対象、`Restore` の対象も、それぞれ識別できる表示にする。
- Restore対象のステッカーは元の色を残す。正解位置には不透明の `R` を表示し、Restore対象ステッカーには半透明の `R` を重ねて移動と向きに追従させる。
- 灰色化したステッカーは、カメラから見て陰になるB・L・D面でも黒ではなく灰色と識別できる明るさを保つ。
- カメラのどのプリセット視点でも確認できるよう、U・R・F・D・L・Bの全6面に面名を表示する。
- Tutorialタブ内に、問題記法、色付き追跡ステッカー、`G`、`V`、`F`、`R`、`X`、`O`、灰色ステッカー、面名、視点変更の意味を初見の利用者向けに常時表示する。
- 未達状態は大きな `X`、達成状態は大きな `O` で表示する。
- 音声通知は初期実装の対象外とする。

## Initial Problems

初期実装では次の問題を、この順序で出題する。最初の5問をPosition問題の導入、その後をSticker問題とする。

1. `ULF corner -> URB corner`
2. `BDR corner -> URB corner`
3. `FLD corner -> ULF corner`
4. `FLD corner -> URB corner`
5. `FLD corner -> URB corner fix UB edge`
6. `ULF -> URB`
7. `FLD -> URB via ULF`
8. `BDR -> URB`
9. `FLD -> URB fix UB edge`
10. `FLD -> URB fix UB edge restore UR`
11. `FLD -> URB restore UB fix RB edge`

各問題の意味は次のとおり。

- `ULF corner -> URB corner`: ULFコーナーパーツを、向きを問わずURBコーナー位置へ移す。
- `BDR corner -> URB corner`: BDRコーナーパーツを、向きを問わずURBコーナー位置へ移す。
- `FLD corner -> ULF corner`: FLDコーナーパーツを、向きを問わずULFコーナー位置へ移す。
- `FLD corner -> URB corner`: FLDコーナーパーツを、向きを問わずURBコーナー位置へ移す。
- `FLD corner -> URB corner fix UB edge`: UBエッジを一度も動かさず、FLDコーナーパーツを向きを問わずURBコーナー位置へ移す。
- `ULF -> URB`: ULFのUステッカーをURB位置のU面へ移す。
- `FLD -> URB via ULF`: FLDのFステッカーをURB位置のU面へ移し、その途中でULF位置のU面を一度以上通過させる。
- `BDR -> URB`: BDRのBステッカーをURB位置のU面へ移す。
- `FLD -> URB fix UB edge`: FLDのFステッカーをURB位置のU面へ移し、試行中はUBエッジを一度も動かさない。
- `FLD -> URB fix UB edge restore UR`: FLDのFステッカーをURB位置のU面へ移し、試行中はUBエッジを一度も動かさず、Goal成立時には開始時のUR位置のUステッカーを元の位置・向きへ戻す。
- `FLD -> URB restore UB fix RB edge`: FLDのFステッカーをURB位置のU面へ移し、試行中はRBエッジを一度も動かさず、Goal成立時には開始時のUB位置のUステッカーを元の位置・向きへ戻す。

## Camera

視点変更は観察だけを目的とし、キューブの操作として扱わない。視点を変えても盤面、手順履歴、手数、各条件の判定には影響しない。
キューブ全体の持ち替え・全体回転は操作として提供しない。
